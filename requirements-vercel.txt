# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
httpx==0.25.2
aiofiles==23.2.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Logging and monitoring
structlog==23.2.0
prometheus-client==0.19.0

# ML dependencies (lightweight versions for Vercel)
numpy==1.24.3
scikit-learn==1.3.0

# Utilities
tenacity==8.2.3

# Note: Excluded packages that may not work well in Vercel:
# - redis (use Vercel KV or external Redis service)
# - lightgbm (too large for serverless)
# - pandas (use lighter alternatives if needed)
# - asyncio-mqtt (use external MQTT service)
# - pytest, black, isort, mypy (dev dependencies)
